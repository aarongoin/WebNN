WebNN/
    README.md
    package.json
    __tests__/
    lib/
        server/ 
        layers/
        util/
    models/
        [model_name]/
            config.js
            model.js
            weights.bin
            validation.csv
            

Validation protocol:
    validateWeights(weights) => float accuracy

DataSource protocol:
    getBatch(size = 1) => Float32Array minibatch

class Validator implements Validation:
    constructor(DataSource)
    validate(weights)

class BinaryDataSource implements DataSource:
    constructor(path, options)
    getBatch(size)

class CSVDataSource implements DataSource:
    constructor(path, options)
    getBatch(size)

class LegacyDataSource implements DataSource:
    constructor(path, options)
    getBatch(size)